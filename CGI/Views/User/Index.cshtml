
@model List<User>

<h1>Users:</h1>

<table>
    <thead>
        <tr>
            <th>Id</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.UserId</td>
            </tr>
        }
    </tbody>
</table>